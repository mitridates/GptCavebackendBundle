{#
/**
 * @file edit_ajax.html.twig
 * Carga un formulario en un tab
 * Cargada mediante javascript muestra un resultado directo del controlador.
 *
 * @param Cave cave
 * @param FormView form Formulario de edición
 * @param string name Nombre (cave o sufijo de las entidades relacionadas con Cave) con el que solicitamos el FormType/entidad... que queremos cargar
 * @param array arrayParams
 * @param array onetoone Formularios para las relacines onetoone con Cave
 * @param array onetomany Formularios para las relacines onetomany con Cave
 * @param string delete_token CsrfToken
 */
#}
{%if name in onetomany %}
{%set path = '@GptCavebackend/content/cave/forms/onetomany.html.twig'%}
{%else%}
{%set path = '@GptCavebackend/content/cave/forms/onetoone.html.twig'%}
{%endif%}    
{%include path with { 'name': name, 'form': form, 'cave' : cave,  'delete_token': delete_token, 'xparams': xparams} only %}