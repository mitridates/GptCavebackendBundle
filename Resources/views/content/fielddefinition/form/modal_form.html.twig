{#
/**
 * Edit Fielddefinitionlang
 * @file modal.html.twig
 * @param array arrayParams
 * @param FormView form
 * @param string language
 * @param Fielddefinitionlang entity
 * @param Fielddefinition fielddefinition
 * @param bool isNewFielddefinitionlang Checkked if is new translation in controller
 */
#}
{% set panelType =  isNewFielddefinitionlang ? arrayParams.page.style.form_panel_new : arrayParams.page.style.form_panel_edit %}
{% set paneltitle =  isNewFielddefinitionlang ? 'fielddefinition.modal.new.translation' : 'fielddefinition.modal.edit.translation' %}
{% include '@GptCavebackend/partial/form/error/all_errors_message.html.twig'%}
{% form_theme form '@GptCavebackend/symfony/fields.html.twig' %}
{{ form_start(form) }}
{% embed '@GptCavebackend/partial/page/panel.html.twig' with {type: panelType } %}
    {% block heading %}{{ paneltitle|trans({},'cavepages')|raw -}}{% endblock heading %}
    {% block body %}
        <table class="table">
            <tr>
                <td>{{ form_label(form.name)}}</td>
                <td>{{ form_label(form.abbreviation)}}</td>
                <td>{{ form_label(form.review)}}</td>
            </tr>
            <tr>
                <td>{{ form_widget(form.name)}}</td>
                <td>{{ form_widget(form.abbreviation)}}</td>
                <td>{{ form_widget(form.review)}}</td>
            </tr>
        </table>
        {%set attr = { 'attr': {'style': 'height: 250px'} } %}
        <table class="table">
            <tr>
                <td>{{ form_label(form.definition)}}</td>
                <td>{{ form_label(form.example)}}</td>
                <td>{{ form_label(form.comment)}}</td>
                <td>{{ form_label(form.uso)}}</td>
            </tr>
            <tr>
                <td>{{ form_widget(form.definition, attr)}}</td>
                <td>{{ form_widget(form.example, attr)}}</td>
                <td>{{ form_widget(form.comment, attr)}}</td>
                <td>{{ form_widget(form.uso, attr)}}</td>
            </tr>
        </table>
    {% endblock body %}
{% endembed %}{{ form_end(form) }}